node_modules/
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts
# .env is committed on purpose: it holds only the local SQLite path, no secrets,
# and the documented startup (`npx prisma migrate dev`) needs it present.
.env*.local
prisma/dev.db
prisma/dev.db-journal
*.db
*.db-journal
.DS_Store
